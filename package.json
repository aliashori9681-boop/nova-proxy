{
  "name": "nova-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Nova Proxy — one-click Cloudflare Worker proxy (VLESS/Trojan/SS) with a self-contained bilingual dashboard and per-ISP clean-IP optimization.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.45.0"
  }
}
